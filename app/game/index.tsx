import { Image, StyleSheet, Platform } from 'react-native';

export default function GameScreen() {
  //   navigation.navigate('GameResult', { result: true });
  //   navigation.navigate('GameResult', { result: false });

  return (
    // play game
  );
}
